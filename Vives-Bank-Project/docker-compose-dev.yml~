services:
  postgres_dev:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=my_dev_db
    ports:
      - 5432:5432
    volumes: